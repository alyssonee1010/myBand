# Production API used for App Store / Play Store builds
VITE_API_BASE_URL=https://api.your-domain.com/api

# Optional native dev API for simulator/device testing before production
# Use your Mac's local IP for a real device if localhost does not resolve
VITE_NATIVE_DEV_API_BASE_URL=http://localhost:3001/api
